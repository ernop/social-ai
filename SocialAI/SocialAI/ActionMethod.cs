namespace SocialAi
{

    /// <summary>
    /// The function of the program
    /// manually set value to 2 to one-time download all historical prompts to a textfile
    /// The main method you should run it would be 1 though.
    /// </summary>
    public enum ActionMethod
    {
        BackfillAndMonitor = 1,
        SavePrompts = 2,
    }
}
